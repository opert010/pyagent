services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./knowledge:/app/knowledge
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
